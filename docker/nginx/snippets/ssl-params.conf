# SSL/TLS প্যারামিটার স্নিপেট — HTTPS সার্ভার ব্লকে include হবে (পরের ধাপে তৈরি হবে)
# সার্টিফিকেট পাথ Certbot-এর ডিফল্ট কনভেনশন অনুসরণ করে (SSL ধাপে বিস্তারিত সেটআপ)

ssl_protocols TLSv1.2 TLSv1.3;
ssl_prefer_server_ciphers off;
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;

ssl_session_timeout 1d;
ssl_session_cache shared:SSL:10m;
ssl_session_tickets off;

# OCSP stapling — ক্লায়েন্টকে সার্টিফিকেট রিভোকেশন-স্ট্যাটাস দ্রুত যাচাই করতে সাহায্য করে
ssl_stapling on;
ssl_stapling_verify on;
